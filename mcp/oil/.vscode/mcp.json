{
  "servers": {
		"oil": {
			"type": "stdio",
			"command": "node",
			"args": ["mcp/oil/dist/index.js"],
			"env": {
				"OBSIDIAN_VAULT_PATH": "${input:obsidianVaultPath}"
			}
		},

	},
	"inputs": [
		{
			"id": "obsidianVaultPath",
			"type": "promptString",
			"description": "Absolute path to your Obsidian vault directory",
			"default": "${env:OBSIDIAN_VAULT_PATH}"
		}
	]
}